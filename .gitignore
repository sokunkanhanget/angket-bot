.env
.venv/
venv/
*.pyc
scan_logs.db
# WAL mode keeps two sidecar files next to the database (-shm shared
# memory, -wal write-ahead log). They are as generated as the .db itself
# and must never be tracked - `scan_logs.db` alone did not match them.
scan_logs.db-shm
scan_logs.db-wal
*.db-shm
*.db-wal
*.log
logs/
__pycache__/
.pytest_cache
.pytest_cache/
*.doc
*_files/
*test.md
.claude
session-summary/
next-gen-test/
ONBOARDING.md